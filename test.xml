# Insert just before "2.3 XTTS v2" paragraph
vits_img = img_block("rId50", CW, 2950498, 10, "VITS Model Architecture")
vits_cap = caption_block("Figure 2.1: VITS Model Architecture (Text Encoder → Flow Model → HiFi-GAN Decoder)")
insert1 = vits_img + "\n" + vits_cap + "\n"
old1 = '        <w:t>2.3 XTTS v2 and Voice Cloning</w:t>'
doc = doc.replace(old1, insert1 + "        <w:t>2.3 XTTS v2 and Voice Cloning</w:t>", 1)

# 2. Insert system architecture image after the bullet point and before "The TTSEngine class..." paragraph
# Insert after the bullet "Next.js Web UI..." paragraph and before "The TTSEngine class..."
sys_img = img_block("rId51", CW, MAX_H, 11, "System Architecture")
sys_cap = caption_block("Figure 5.1: VoiceAPI System Architecture (Three-Tier Architecture)")
insert2 = sys_img + "\n" + sys_cap + "\n"
old2 = '        <w:t>The TTSEngine class (src/engine.py) serves as the central inference hub'
doc = doc.replace(old2, insert2 + "        <w:t>The TTSEngine class (src/engine.py) serves as the central inference hub", 1)

# 3. Insert data flow diagram between Table 5.1 caption and 5.2 heading
dataflow_img = img_block("rId52", CW, MAX_H, 12, "Data Flow Diagram")
dataflow_cap = caption_block("Figure 5.2: VoiceAPI Data Flow Diagram (10-Step Request Lifecycle)")
insert3 = dataflow_img + "\n" + dataflow_cap + "\n"
old3 = '        <w:t>5.2 Use Case Diagram</w:t>'
doc = doc.replace(old3, insert3 + "        <w:t>5.2 Use Case Diagram</w:t>", 1)

# 4. Insert use case diagram after "The system supports two primary user roles:" paragraph
uc_img = img_block("rId53", CW, 3481110, 13, "Use Case Diagram")
uc_cap = caption_block("Figure 5.3: VoiceAPI Use Case Diagram (End User and Developer Roles)")
insert4 = uc_img + "\n" + uc_cap + "\n"
old4 = '        <w:t>The system supports two primary user roles:</w:t>'
doc = doc.replace(old4, "        <w:t>The system supports two primary user roles:</w:t>" + "\n" + "    </w:r>\n    </w:p>\n" + insert4 + "    <w:p w:rsidR=\"005F7E1A\">", 1)
# Actually this approach is risky - let's do it differently, insert before 5.2.1
old4b = '        <w:t>5.2.1 End User (Web UI)</w:t>'
doc = doc.replace(old4b, insert4 + "        <w:t>5.2.1 End User (Web UI)</w:t>", 1)

# 5. Insert training pipeline before 5.3 Module Design heading
train_img = img_block("rId54", CW, MAX_H, 14, "Training Pipeline")
train_cap = caption_block("Figure 5.4: VoiceAPI Training Pipeline (Data Prep → Training → Export → Output)")
insert5 = train_img + "\n" + train_cap + "\n"
old5 = '        <w:t>5.3 Module Design</w:t>'
doc = doc.replace(old5, insert5 + "        <w:t>5.3 Module Design</w:t>", 1)

# 6. Insert voice map after Table 7.1 caption before 7.2 heading
voice_img = img_block("rId55", CW, MAX_H, 15, "Voice Map")
voice_cap = caption_block("Figure 7.1: VoiceAPI – 21 Voices across 11 Languages")
insert6 = voice_img + "\n" + voice_cap + "\n"
old6 = '        <w:t>7.2 Performance Benchmarks</w:t>'
doc = doc.replace(old6, insert6 + "        <w:t>7.2 Performance Benchmarks</w:t>", 1)
